name: fibrosis_quantification_software
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.9
  - pip
  - prefect
  - pip:
    - flake8
    - python-dotenv>=0.5.1
    - pytest
    - pytest-cov
    - black
    - streamlit
